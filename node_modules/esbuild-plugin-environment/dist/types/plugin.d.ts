import type { Plugin } from 'esbuild';
export declare const PLUGIN_NAME = "environment-plugin";
type StringRecord = Record<string, string>;
export declare const environmentPlugin: (data: string[] | StringRecord) => Plugin;
export {};
