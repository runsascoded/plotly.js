import { convertToDefineObject } from './utils.js';
export const PLUGIN_NAME = 'define-plugin';
export const definePlugin = (data) => {
    return {
        name: PLUGIN_NAME,
        setup(build) {
            Object.assign((build.initialOptions.define ??= {}), convertToDefineObject(data));
        },
    };
};
