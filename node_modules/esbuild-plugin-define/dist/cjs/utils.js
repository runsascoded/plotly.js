"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertToDefineObject = exports.makeKey = void 0;
const makeKey = (...inputs) => inputs.filter(input => !!input).join('.');
exports.makeKey = makeKey;
function convertToDefineObject(data) {
    const convertToEntries = (data, prefix = '') => {
        return Object.entries(data).reduce((entries, [key, value]) => {
            var _a;
            const newKey = (0, exports.makeKey)(prefix, key);
            if (value && typeof value === 'object' && !Array.isArray(value)) {
                entries.push(...convertToEntries(value, newKey));
            }
            else {
                entries.push([newKey, (_a = JSON.stringify(value)) !== null && _a !== void 0 ? _a : String(undefined)]);
            }
            return entries;
        }, []);
    };
    return Object.fromEntries(convertToEntries(data));
}
exports.convertToDefineObject = convertToDefineObject;
