declare module "extra-iterable" {

export type tillFn = (dones: boolean[]) => boolean;
export type reduceFn<T, U> = (acc: U, v: T, i: number, x: Iterable<T>) => U;
export type calledFn<T> = (v: T, i: number, x: Iterable<T>) => void;
export type testFn<T> = (v: T, i: number, x: Iterable<T>) => boolean;
export type mapFn<T, U> = (v: T, i: number, x: Iterable<T>) => U;
export type combineFn<T> = (a: T, b: T) => T;
export type compareFn<T> = (a: T, b: T) => number;
/**
 * Produces accumulating values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/accumulate)
 * @param x an iterable
 * @param fr reduce function (acc, v, i, x)
 * @param acc initial value
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * [...accumulate(x, (acc, v) => acc+v)];
 * // [ 1, 3, 6, 10 ]
 * 
 * [...accumulate(x, (acc, v) => acc+v, 100)];
 * // [ 101, 103, 106, 110 ]
 * ```
 */
export function accumulate<T, U = T>(x: Iterable<T>, fr: reduceFn<T, T | U>, acc?: T | U): IterableIterator<T | U>;
/**
 * Gives a function that iterates over values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/callable)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var f = callable(x);
 * 
 * for(var i=0; i<8; i++)
 *   console.log(f());
 * // 1
 * // 2
 * // 3
 * // 4
 * // undefined
 * // undefined
 * // undefined
 * // undefined
 * ```
 */
export function callable<T>(x: Iterable<T>): () => T;
/**
 * Lists cartesian product of iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/cartesianProduct)
 * @param xs iterables
 * @param fm map function (vs, i)
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * var y = [10, 20, 30];
 * [...cartesianProduct([x, y])];
 * // [
 * //   [ 1, 10 ], [ 1, 20 ],
 * //   [ 1, 30 ], [ 2, 10 ],
 * //   [ 2, 20 ], [ 2, 30 ],
 * //   [ 3, 10 ], [ 3, 20 ],
 * //   [ 3, 30 ]
 * // ]
 * 
 * [...cartesianProduct([x, y], ([a, b]) => a + b)];
 * // [
 * //   11, 21, 31, 12, 22,
 * //   32, 13, 23, 33
 * // ]
 * ```
 */
export function cartesianProduct<T, U = T>(xs: Iterable<T>[], fm?: mapFn<T[], T[] | U>): IterableIterator<T[] | U>;
/**
 * Breaks iterable into chunks of given size.
 * [📘](https://github.com/nodef/extra-iterable/wiki/chunk)
 * @param x an iterable
 * @param n chunk size (1)
 * @param s chunk step (n)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5, 6, 7, 8];
 * [...chunk(x, 3)];
 * // [ [ 1, 2, 3 ], [ 4, 5, 6 ], [ 7, 8 ] ]
 * 
 * [...chunk(x, 2, 3)];
 * // [ [ 1, 2 ], [ 4, 5 ], [ 7, 8 ] ]
 * 
 * [...chunk(x, 4, 3)];
 * // [ [ 1, 2, 3, 4 ], [ 4, 5, 6, 7 ], [ 7, 8 ] ]
 * ```
 */
export function chunk<T>(x: Iterable<T>, n?: number, s?: number): IterableIterator<T[]>;
/**
 * Compares two iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/compare)
 * @param x an iterable
 * @param y another iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns x<y: -1, x=y: 0, x>y: 1
 * @example
 * ```javascript
 * var x = [1, 2];
 * var y = [1, 2, 3];
 * compare(x, y);
 * // -1
 * 
 * var y = [1, 2];
 * compare(x, y);
 * // 0
 * 
 * var y = [1, -2];
 * compare(x, y);
 * // 1
 * 
 * compare(x, y, (a, b) => Math.abs(a) - Math.abs(b));
 * // 0
 * 
 * compare(x, y, null, v => Math.abs(v));
 * // 0
 * ```
 */
export function compare<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): number;
/**
 * Appends values from iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/concat)
 * @param xs iterables
 * @example
 * ```javascript
 * var x = [1, 2];
 * var y = [3, 4];
 * [...concat(x, y)];
 * // [ 1, 2, 3, 4 ]
 * 
 * [...concat(x, y, y)];
 * // [ 1, 2, 3, 4, 3, 4 ]
 * ```
 */
export function concat<T>(...xs: Iterable<T>[]): IterableIterator<T>;
/**
 * Copies part of iterable to another.
 * [📘](https://github.com/nodef/extra-iterable/wiki/copy)
 * @param x target iterable
 * @param y source iterable
 * @param j write index (0)
 * @param i read start index (0)
 * @param I read end index (X)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * var y = [10, 20, 30];
 * [...copy(x, y)];
 * // [ 10, 20, 30, 4, 5 ]
 * 
 * [...copy(x, y, 1)];
 * // [ 1, 10, 20, 30, 5 ]
 * 
 * [...copy(x, y, 1, 1)];
 * // [ 1, 20, 30, 4, 5 ]
 * 
 * [...copy(x, y, 1, 1, 2)];
 * // [ 1, 20, 3, 4, 5 ]
 * ```
 */
export function copy<T>(x: Iterable<T>, y: Iterable<T>, j?: number, i?: number, I?: number): IterableIterator<T>;
/**
 * Copies part of iterable within.
 * [📘](https://github.com/nodef/extra-iterable/wiki/copyWithin)
 * @param x an iterable
 * @param j write index (0)
 * @param i read start index (0)
 * @param I read end index (X)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...copyWithin(x, 3)];
 * // [ 1, 2, 3, 1, 2 ]
 * 
 * [...copyWithin(x, 3, 1)];
 * // [ 1, 2, 3, 2, 3 ]
 * 
 * [...copyWithin(x, 3, 1, 2)];
 * // [ 1, 2, 3, 2, 5 ]
 * ```
 */
export function copyWithin<T>(x: Iterable<T>, j?: number, i?: number, I?: number): IterableIterator<T>;
/**
 * Counts values which satisfy a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/count)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 1, 2, 2, 4];
 * count(x, v => v % 2 === 1);
 * // 2
 * 
 * count(x, v => v % 2 === 0);
 * // 3
 * ```
 */
export function count<T>(x: Iterable<T>, ft: testFn<T>): number;
/**
 * Counts occurrences of values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/countAs)
 * @param x an iterable
 * @param fm map function (v, i, x)
 * @returns Map {value => count}
 * @example
 * ```javascript
 * var x = [1, 1, 2, 2, 4];
 * countAs(x);
 * // Map(3) { 1 => 2, 2 => 2, 4 => 1 }
 * 
 * var x = [1, 2, 3, 4];
 * countAs(x, v => v % 2);
 * // Map(2) { 1 => 2, 0 => 2 }
 * ```
 */
export function countAs<T, U = T>(x: Iterable<T>, fm?: mapFn<T, T | U>): Map<T | U, number>;
/**
 * Breaks iterable when test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/cut)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...cut(x, v => v % 2 === 0)];
 * // [ [ 1 ], [ 2, 3 ], [ 4, 5 ] ]
 * 
 * [...cut(x, v => v % 2 === 1)];
 * // [ [], [ 1, 2 ], [ 3, 4 ], [ 5 ] ]
 * ```
 */
export function cut<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T[]>;
/**
 * Breaks iterable at given indices.
 * [📘](https://github.com/nodef/extra-iterable/wiki/cutAt)
 * @param x an iterable
 * @param is split indices (sorted)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...cutAt(x, [1, 3])];
 * // [ [ 1 ], [ 2, 3 ], [ 4, 5 ] ]
 * 
 * [...cutAt(x, [0, 4])];
 * // [ [], [ 1, 2, 3, 4 ], [ 5 ] ]
 * ```
 */
export function cutAt<T>(x: Iterable<T>, is: Iterable<number>): IterableIterator<T[]>;
/**
 * Breaks iterable after given indices.
 * [📘](https://github.com/nodef/extra-iterable/wiki/cutAtRight)
 * @param x an iterable
 * @param is split indices (sorted)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...cutAtRight(x, [1, 3])];
 * // [ [ 1, 2 ], [ 3, 4 ], [ 5 ] ]
 * 
 * [...cutAtRight(x, [0, 4])];
 * // [ [ 1 ], [ 2, 3, 4, 5 ], [] ]
 * ```
 */
export function cutAtRight<T>(x: Iterable<T>, is: Iterable<number>): IterableIterator<T[]>;
/**
 * Breaks iterable after test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/cutRight)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...cutRight(x, v => v % 2 === 0)];
 * // [ [ 1, 2 ], [ 3, 4 ], [ 5 ] ]
 * 
 * [...cutRight(x, v => v % 2 === 1)];
 * // [ [ 1 ], [ 2, 3 ], [ 4, 5 ], [] ]
 * ```
 */
export function cutRight<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T[]>;
/**
 * Gives values that cycle through an iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/cycle)
 * @param x an iterable
 * @param i start index (0)
 * @param n number of values (-1 => Inf)
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...cycle(x, 0, 2)];
 * // [ 1, 2 ]
 * 
 * [...cycle(x, 0, 4)];
 * // [ 1, 2, 3, 1 ]
 * 
 * [...cycle(x, 1, 6)];
 * // [ 2, 3, 1, 2, 3, 1 ]
 * ```
 */
export function cycle<T>(x: Iterable<T>, i?: number, n?: number): IterableIterator<T>;
/**
 * Gives values not present in another iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/difference)
 * @param x an iterable
 * @param y another iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * var y = [2, 4];
 * [...difference(x, y)];
 * // [ 1, 3, 5 ]
 * 
 * var y = [-2, -4];
 * [...difference(x, y)];
 * // [ 1, 2, 3, 4, 5 ]
 * 
 * [...difference(x, y, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ 1, 3, 5 ]
 * 
 * [...difference(x, y, null, v => Math.abs(v))];
 * // [ 1, 3, 5 ]
 * ```
 */
export function difference<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<T>;
/**
 * Discards first n values only.
 * [📘](https://github.com/nodef/extra-iterable/wiki/drop)
 * @param x an iterable
 * @param n number of values (1)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...drop(x, 2)];
 * // [ 3, 4, 5 ]
 * 
 * [...drop(x, 3)];
 * // [ 4, 5 ]
 * ```
 */
export function drop<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Discards last n values only.
 * [📘](https://github.com/nodef/extra-iterable/wiki/dropRight)
 * @param x an iterable
 * @param n number of values (1)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...dropRight(x, 2)];
 * // [ 1, 2, 3 ]
 * 
 * [...dropRight(x, 3)];
 * // [ 1, 2 ]
 * ```
 */
export function dropRight<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Discards values from left, while a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/dropWhile)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...dropWhile(x, v => v < 3)];
 * // [ 3, 4, 5 ]
 * 
 * [...dropWhile(x, v => v < 4)];
 * // [ 4, 5 ]
 * ```
 */
export function dropWhile<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T>;
/**
 * Discards values from right, while a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/dropWhileRight)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...dropWhileRight(x, v => v >= 3)];
 * // [ 1, 2 ]
 * 
 * [...dropWhileRight(x, v => v >= 4)];
 * // [ 1, 2, 3 ]
 * ```
 */
export function dropWhileRight<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T>;
/**
 * End of iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/END)
 * @example
 * ```javascript
 * END;
 * // 9007199254740991 (Number.MAX_SAFE_INTEGER)
 * // (use this to mark end index of an iterable)
 * ```
 */
export const END: number;
/**
 * Lists all index-value pairs.
 * [📘](https://github.com/nodef/extra-iterable/wiki/entries)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = ["A", "B", "C"];
 * [...entries(x)];
 * // [ [ 0, "A" ], [ 1, "B" ], [ 2, "C" ] ]
 * ```
 */
export function entries<T>(x: Iterable<T>): IterableIterator<[
	number,
	T
]>;
/**
 * Checks if all values satisfy a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/every)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, -3, -4];
 * every(x, v => v > 0);
 * // false
 * 
 * every(x, v => v > -10);
 * // true
 * ```
 */
export function every<T>(x: Iterable<T>, ft?: testFn<T>): boolean;
/**
 * Fills with given value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/fill)
 * @param x an iterable
 * @param v value
 * @param i start index (0)
 * @param I end index (X)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * [...fill(x, 2)];
 * // [ 2, 2, 2, 2 ]
 * 
 * [...fill(x, 2, 1)];
 * // [ 1, 2, 2, 2 ]
 * 
 * [...fill(x, 2, 1, 3)];
 * // [ 1, 2, 2, 4 ]
 * ```
 */
export function fill<T>(x: Iterable<T>, v: T, i?: number, I?: number): IterableIterator<T>;
/**
 * Keeps the values which pass a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/filter)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...filter(x, v => v % 2 === 1)];
 * // [ 1, 3, 5 ]
 * 
 * [...filter(x, v => v % 2 === 0)];
 * // [ 2, 4 ]
 * ```
 */
export function filter<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T>;
/**
 * Keeps the values at given indices.
 * [📘](https://github.com/nodef/extra-iterable/wiki/filterAt)
 * @param x an iterable
 * @param is indices
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * [...filterAt(x, [1, 2])];
 * // [ 4, 6 ]
 * 
 * [...filterAt(x, [1, 3])];
 * // [ 4, 8 ]
 * ```
 */
export function filterAt<T>(x: Iterable<T>, is: number[]): IterableIterator<T>;
/**
 * Finds first value passing a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/find)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * find(x, v => v % 2 == 0);
 * // 2        ^
 * 
 * find(x, v => v % 2 == 1);
 * // 1     ^
 * ```
 */
export function find<T>(x: Iterable<T>, ft: testFn<T>): T;
/**
 * Finds all values passing a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/findAll)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...findAll(x, v => v % 2 === 1)];
 * // [ 1, 3, 5 ]
 * 
 * [...findAll(x, v => v % 2 === 0)];
 * // [ 2, 4 ]
 * ```
 */
export function findAll<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T>;
/**
 * Finds last value passing a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/findRight)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * findRight(x, v => v % 2 == 0);
 * // 4              ^
 * 
 * findRight(x, v => v % 2 == 1);
 * // 5                 ^
 * ```
 */
export function findRight<T>(x: Iterable<T>, ft: testFn<T>): T;
/**
 * Flattens nested iterable to given depth.
 * [📘](https://github.com/nodef/extra-iterable/wiki/flat)
 * @param x a nested iterable
 * @param n maximum depth (-1 => all)
 * @param fm map function (v, i, x)
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [[1, 2], [3, [4, [5]]]];
 * [...flat(x)];
 * // [ 1, 2, 3, 4, 5 ]
 * 
 * [...flat(x, 1)];
 * // [ 1, 2, 3, [ 4, [ 5 ] ] ]
 * 
 * [...flat(x, 2)];
 * // [ 1, 2, 3, 4, [ 5 ] ]
 * ```
 */
export function flat(x: Iterable<any>, n?: number, fm?: mapFn<any, any>, ft?: testFn<any>): IterableIterator<any>;
/**
 * Flattens nested iterable, based on map function.
 * [📘](https://github.com/nodef/extra-iterable/wiki/flatMap)
 * @param x a nested iterable
 * @param fm map function (v, i, x)
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [[1, 2], [3, [4, [5]]]];
 * [...flatMap(x)];
 * // [ 1, 2, 3, [ 4, [ 5 ] ] ]
 * 
 * [...flatMap(x, v => flat(v, 1))];
 * // [ 1, 2, 3, 4, [ 5 ] ]
 * 
 * [...flatMap(x, v => flat(v))];
 * // [ 1, 2, 3, 4, 5 ]
 * ```
 */
export function flatMap(x: Iterable<any>, fm?: mapFn<any, any>, ft?: testFn<any>): IterableIterator<any>;
/**
 * Calls a function for each value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/forEach)
 * @param x an iterable
 * @param fc called function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, -3, -4];
 * forEach(x, v => console.log(v));
 * // 1
 * // 2
 * // -3
 * // -4
 * ```
 */
export function forEach<T>(x: Iterable<T>, fc: calledFn<T>): void;
/**
 * Converts iterator to iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/from)
 * @param x an iterator/iterable
 * @example
 * ```javascript
 * var x = [1, 2].values();
 * var xi = x[Symbol.iterator]();
 * [...from(xi)];
 * // [ 1, 2 ]
 * ```
 */
export function from<T>(x: Iterator<T> | Iterable<T>): Iterable<T>;
/**
 * Generates iterable from function application.
 * [📘](https://github.com/nodef/extra-iterable/wiki/fromApply)
 * @param fm map function (v, i)
 * @param v start value
 * @param n number of values (-1 => Inf)
 * @example
 * ```javascript
 * [...fromApply(v => v+2, 2, 4)];
 * // [ 2, 4, 6, 8 ]
 * 
 * [...fromApply(v => v*2, 2, 4)];
 * // [ 2, 4, 8, 16 ]
 * ```
 */
export function fromApply<T>(fm: mapFn<T, T>, v: T, n?: number): IterableIterator<T>;
/**
 * Generates iterable from function call.
 * [📘](https://github.com/nodef/extra-iterable/wiki/fromCall)
 * @param fc called function
 * @param as arguments
 * @example
 * ```javascript
 * var x = fromCall(Math.random);
 * [...take(x, 4)];
 * // [
 * //   0.9563611116619364,
 * //   0.9938159365212336,
 * //   0.38494305694810205,
 * //   0.06828624643528514
 * // ]
 * ```
 */
export function fromCall<T>(fc: Function, ...as: any[]): IterableIterator<T>;
/**
 * Generates iterable from given number range.
 * [📘](https://github.com/nodef/extra-iterable/wiki/fromRange)
 * @param v start number (0)
 * @param V stop number, excluding (end)
 * @param s step size (1)
 * @example
 * ```javascript
 * [...fromRange(0, 4)];
 * // [ 0, 1, 2, 3 ]
 * 
 * [...fromRange(0, 8, 2)];
 * // [ 0, 2, 4, 6 ]
 * ```
 */
export function fromRange(v?: number, V?: number, s?: number): IterableIterator<number>;
/**
 * Gets value at index.
 * [📘](https://github.com/nodef/extra-iterable/wiki/get)
 * @param x an iterable
 * @param i index
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * get(x, 1);
 * // 4
 * 
 * get(x, 3);
 * // 8
 * ```
 */
export function get<T>(x: Iterable<T>, i: number): T;
/**
 * Gets values at indices.
 * [📘](https://github.com/nodef/extra-iterable/wiki/getAll)
 * @param x an iterable
 * @param is indices (sorted)
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * [...getAll(x, [1, 2])];
 * // [ 4, 6 ]
 * 
 * [...getAll(x, [1, 3])];
 * // [ 4, 8 ]
 * ```
 */
export function getAll<T>(x: Iterable<T>, is: Iterable<number>): IterableIterator<T>;
/**
 * Gets value at path in a nested iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/getPath)
 * @param x a nested iterable
 * @param p path
 * @example
 * ```javascript
 * var x = [[2, 4], 6, 8];
 * getPath(x, [1]);
 * // 6
 * 
 * getPath(x, [0, 1]);
 * // 4
 * 
 * getPath(x, [0, 1, 2]);
 * // undefined
 * ```
 */
export function getPath(x: Iterable<any>, p: number[]): any;
/**
 * Keeps similar values together and in order.
 * [📘](https://github.com/nodef/extra-iterable/wiki/group)
 * @param x an iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 2, -2, -2, 4];
 * [...group(x)];
 * // [ [ 1 ], [ 2, 2 ], [ -2, -2 ], [ 4 ] ]
 * 
 * [...group(x, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ [ 1 ], [ 2, 2, -2, -2 ], [ 4 ] ]
 * 
 * [...group(x, null, v => Math.abs(v))];
 * // [ [ 1 ], [ 2, 2, -2, -2 ], [ 4 ] ]
 * ```
 */
export function group<T, U = T>(x: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<T[]>;
/**
 * Checks if iterable contains an infix.
 * [📘](https://github.com/nodef/extra-iterable/wiki/hasInfix)
 * @param x an iterable
 * @param y infix?
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * hasInfix(x, [2, 3]);
 * // true
 * 
 * hasInfix(x, [-2, -3]);
 * // false
 * 
 * hasInfix(x, [-2, -3], (a, b) => Math.abs(a) - Math.abs(b));
 * // true
 * 
 * hasInfix(x, [-2, -3], null, v => Math.abs(v));
 * // true
 * ```
 */
export function hasInfix<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Checks if nested iterable has a path.
 * [📘](https://github.com/nodef/extra-iterable/wiki/hasPath)
 * @param x a nested iterable
 * @param p path
 * @example
 * ```javascript
 * var x = [[2, 4], 6, 8];
 * hasPath(x, [1]);
 * // true
 * 
 * hasPath(x, [0, 1]);
 * // true
 * 
 * hasPath(x, [0, 1, 2]);
 * // false
 * ```
 */
export function hasPath(x: Iterable<any>, p: number[]): boolean;
/**
 * Checks if iterable starts with a prefix.
 * [📘](https://github.com/nodef/extra-iterable/wiki/hasPrefix)
 * @param x an iterable
 * @param y prefix?
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * hasPrefix(x, [1, 2]);
 * // true
 * 
 * hasPrefix(x, [-1, -2]);
 * // false
 * 
 * hasPrefix(x, [-1, -2], (a, b) => Math.abs(a) - Math.abs(b));
 * // true
 * 
 * hasPrefix(x, [-1, -2], null, v => Math.abs(v));
 * // true
 * ```
 */
export function hasPrefix<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Checks if iterable has a subsequence.
 * [📘](https://github.com/nodef/extra-iterable/wiki/hasSubsequence)
 * @param x an iterable
 * @param y subsequence?
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * hasSubsequence(x, [2, 4]);
 * // true
 * 
 * hasSubsequence(x, [-2, -4]);
 * // false
 * 
 * hasSubsequence(x, [-2, -4], (a, b) => Math.abs(a) - Math.abs(b));
 * // true
 * 
 * hasSubsequence(x, [-2, -4], null, v => Math.abs(v));
 * // true
 * ```
 */
export function hasSubsequence<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Checks if iterable ends with a suffix.
 * [📘](https://github.com/nodef/extra-iterable/wiki/hasSuffix)
 * @param x an iterable
 * @param y suffix?
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * hasSuffix(x, [3, 4]);
 * // true
 * 
 * hasSuffix(x, [-3, -4]);
 * // false
 * 
 * hasSuffix(x, [-3, -4], (a, b) => Math.abs(a) - Math.abs(b));
 * // true
 * 
 * hasSuffix(x, [-3, -4], null, v => Math.abs(v));
 * // true
 * ```
 */
export function hasSuffix<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Checks if iterable has a value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/hasValue)
 * @param x an iterable
 * @param v value?
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, -3];
 * hasValue(x, 3);
 * // false
 * 
 * hasValue(x, 3, (a, b) => Math.abs(a) - Math.abs(b));
 * // true
 * 
 * hasValue(x, 3, null, v => Math.abs(v));
 * // true
 * ```
 */
export function hasValue<T, U = T>(x: Iterable<T>, v: T, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Gets first value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/head)
 * @param x an iterable
 * @param vd default value
 * @example
 * ```javascript
 * head([1, 2, 3]);
 * // 1
 * 
 * head([], -1);
 * // -1
 * ```
 */
export function head<T>(x: Iterable<T>, vd?: T): T;
/**
 * Checks if iterable has a value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/includes)
 * @param x an iterable
 * @param v search value
 * @param i start index (0)
 * @example
 * ```javascript
 * var x = [1, 2, -3];
 * includes(x, 3);
 * // false
 * 
 * includes(x, -3);
 * // true
 * ```
 */
export function includes<T>(x: Iterable<T>, v: T, i?: number): boolean;
/**
 * Gets zero-based index.
 * [📘](https://github.com/nodef/extra-iterable/wiki/index)
 * @param x an iterable
 * @param i index (-ve: from right) (0)
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * index(x, 1);
 * // 1
 * 
 * index(x, -1);
 * // 3
 * 
 * index(x, -10);
 * // 0
 * ```
 */
export function index<T>(x: Iterable<T>, i?: number): number;
/**
 * Finds first index of a value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/indexOf)
 * @param x an iterable
 * @param v search value
 * @param i start index (0)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 2, 5];
 * indexOf(x, 2);
 * // 1        ^
 * 
 * var x = [1, 2, 3, 2, 5];
 * indexOf(x, 2, 2);
 * // 3              ^
 * ```
 */
export function indexOf<T>(x: Iterable<T>, v: T, i?: number): number;
/**
 * Gets index range of part of iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/indexRange)
 * @param x an iterable
 * @param i start index (-ve: from right) (0)
 * @param I end index (-ve: from right) (X)
 * @returns [start index, end index]
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * indexRange(x, 1);
 * // [1, 4]
 * 
 * indexRange(x, 1, -1);
 * // [1, 3]
 * 
 * indexRange(x, 1, -10);
 * // [1, 1]
 * ```
 */
export function indexRange<T>(x: Iterable<T>, i?: number, I?: number): [
	number,
	number
];
/**
 * Gets values except last.
 * [📘](https://github.com/nodef/extra-iterable/wiki/init)
 * @param x an iterable
 * @example
 * ```javascript
 * [...init([1, 2, 3])];
 * // [1, 2]
 * 
 * [...init([1])];
 * // []
 * ```
 */
export function init<T>(x: Iterable<T>): IterableIterator<T>;
/**
 * Merges values from iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/interleave)
 * @param xs iterables
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var y = [10, 20, 30, 40];
 * [...interleave([x, y])];
 * // [ 1, 10, 2, 20, 3, 30, 4, 40 ]
 * 
 * var y = [10, 20];
 * [...interleave([x, y])];
 * // [ 1, 10, 2, 20, 3, 4 ]
 * ```
 */
export function interleave<T>(xs: Iterable<T>[]): IterableIterator<T>;
/**
 * Places values of an iterable between another.
 * [📘](https://github.com/nodef/extra-iterable/wiki/intermix)
 * @param x an iterable
 * @param y another iterable
 * @param m number of values from x (1)
 * @param n number of values from y (1)
 * @param s step size for x (m)
 * @param t step size for y (n)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var y = [10, 20, 30];
 * [...intermix(x, [10])];
 * // [ 1, 10, 2, 10, 3, 10, 4 ]
 * 
 * [...intermix(x, y)];
 * // [ 1, 10, 2, 20, 3, 30, 4 ]
 * 
 * [...intermix(x, y, 2)];
 * // [ 1, 2, 10, 3, 4 ]
 * 
 * [...intermix(x, y, 1, 2)];
 * // [ 1, 10, 20, 2, 30, 10, 3, 20, 30, 4 ]
 * ```
 */
export function intermix<T>(x: Iterable<T>, y: Iterable<T>, m?: number, n?: number, s?: number, t?: number): IterableIterator<T>;
/**
 * Estimates new values between existing ones.
 * [📘](https://github.com/nodef/extra-iterable/wiki/interpolate)
 * @param x an iterable
 * @param fc combine function (a, b)
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...interpolate(x, (a, b) => (a + b)/2)];
 * // [ 1, 1.5, 2, 2.5, 3 ]
 * ```
 */
export function interpolate<T>(x: Iterable<T>, fc: combineFn<T>): IterableIterator<T>;
/**
 * Gives values present in both iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/intersection)
 * @param x an iterable
 * @param y another iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var y = [2, 3, 5];
 * [...intersection(x, y)];
 * // [ 2, 3 ]
 * 
 * var y = [-2, -3, -5];
 * [...intersection(x, y, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ 2, 3 ]
 * 
 * [...intersection(x, y, null, v => Math.abs(v))];
 * // [ 2, 3 ]
 * ```
 */
export function intersection<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<T>;
/**
 * Places a separator between every value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/intersperse)
 * @param x an iterable
 * @param v separator
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...intersperse(x, 10)];
 * // [ 1, 10, 2, 10, 3 ]
 * 
 * var x = [1, 2, 3, 4];
 * [...intersperse(x, 10)];
 * // [ 1, 10, 2, 10, 3, 10, 4 ]
 * ```
 */
export function intersperse<T>(x: Iterable<T>, v: T): IterableIterator<T>;
/**
 * Checks if value is iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/is)
 * @param v a value
 * @example
 * ```javascript
 * is([1, 2]);
 * // true
 * 
 * is("ab");
 * // true
 * 
 * is(1);
 * // false
 * ```
 */
export function is(v: any): v is Iterable<any>;
/**
 * Checks if iterables have no value in common.
 * [📘](https://github.com/nodef/extra-iterable/wiki/isDisjoint)
 * @param x an iterable
 * @param y another iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * isDisjoint(x, [2, 5]);
 * // false
 * 
 * isDisjoint(x, [-2, -5]);
 * // true
 * 
 * isDisjoint(x, [-2, -5], (a, b) => Math.abs(a) - Math.abs(b));
 * // false
 * 
 * isDisjoint(x, [-2, -5], null, v => Math.abs(v));
 * // false
 * ```
 */
export function isDisjoint<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Checks is iterable is empty.
 * [📘](https://github.com/nodef/extra-iterable/wiki/isEmpty)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * isEmpty(x);
 * // false
 * 
 * var x = [];
 * isEmpty(x);
 * // true
 * ```
 */
export function isEmpty<T>(x: Iterable<T>): boolean;
/**
 * Checks if two iterables are equal.
 * [📘](https://github.com/nodef/extra-iterable/wiki/isEqual)
 * @param x an iterable
 * @param y another iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2];
 * isEqual(x, [1, 2]);
 * // true
 * 
 * isEqual(x, [11, 12]);
 * // false
 * 
 * isEqual(x, [11, 12], (a, b) => (a % 10) - (b % 10));
 * // true
 * 
 * isEqual(x, [11, 12], null, v => v % 10);
 * // true
 * ```
 */
export function isEqual<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Checks if value is iterator.
 * [📘](https://github.com/nodef/extra-iterable/wiki/isIterator)
 * @param v a value
 * @example
 * ```javascript
 * var x = [1, 2];
 * isIterator(x);
 * // false
 * 
 * isIterator(x[Symbol.iterator]());
 * // true
 * 
 * isIterator(x.values());
 * // true
 * ```
 */
export function isIterator(v: any): v is Iterator<any>;
/**
 * Checks if value is list (not string).
 * [📘](https://github.com/nodef/extra-iterable/wiki/isList)
 * @param v a value
 * @example
 * ```javascript
 * isList([1, 2]);
 * // true
 * 
 * isList("ab");
 * // false
 * 
 * isList(1);
 * // false
 * ```
 */
export function isList(v: any): v is Iterable<any>;
/**
 * Checks if value is many iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/isMany)
 * @param v a value
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * isMany(x);
 * // true
 * 
 * isMany(x.values());
 * // false
 * ```
 */
export function isMany(v: any): v is Iterable<any>;
/**
 * Checks if value is once iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/isOnce)
 * @param v a value
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * isOnce(x);
 * // false
 * 
 * isOnce(x.values());
 * // true
 * ```
 */
export function isOnce(v: any): v is IterableIterator<any>;
/**
 * Checks if there are no duplicate values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/isUnique)
 * @param x an iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, -1, -2];
 * isUnique(x);
 * // true
 * 
 * isUnique(x, (a, b) => Math.abs(a) - Math.abs(b));
 * // false
 * 
 * isUnique(x, null, v => Math.abs(v));
 * // false
 * ```
 */
export function isUnique<T, U = T>(x: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): boolean;
/**
 * Gives iterator for iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/iterator)
 * @param x an iterable
 * @example
 * ```javascript
 * iterator([1, 2]);
 * // Object [Array Iterator] {}
 * 
 * iterator(new Set([1, 2]));
 * // [Set Iterator] { 1, 2 }
 * ```
 */
export function iterator<T>(x: Iterable<T>): Iterator<T>;
/**
 * Joins values together.
 * [📘](https://github.com/nodef/extra-iterable/wiki/join)
 * @param x an iterable
 * @param sep separator (,)
 * @example
 * ```javascript
 * var x = [1, 2];
 * join(x);
 * // "1,2"
 * 
 * join(x, " : ");
 * // "1 : 2"
 * ```
 */
export function join<T>(x: Iterable<T>, sep?: string): string;
/**
 * Lists all indices.
 * [📘](https://github.com/nodef/extra-iterable/wiki/keys)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...keys(x)];
 * // [ 0, 1, 2 ]
 * ```
 */
export function keys<T>(x: Iterable<T>): IterableIterator<number>;
/**
 * Gets last value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/last)
 * @param x an iterable
 * @param vd default value
 * @example
 * ```javascript
 * last([1, 2, 3]);
 * // 3
 * 
 * last([], -1);
 * // -1
 * ```
 */
export function last<T>(x: Iterable<T>, vd?: T): T;
/**
 * Finds last index of a value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/lastIndexOf)
 * @param x an iterable
 * @param v search value
 * @param i start index (X-1)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 2, 5];
 * lastIndexOf(x, 2);
 * // 3              ^
 * 
 * var x = [1, 2, 3, 2, 5];
 * lastIndexOf(x, 2, 2);
 * // 1        ^
 * ```
 */
export function lastIndexOf<T>(x: Iterable<T>, v: T, i?: number): number;
/**
 * Gets values from left.
 * [📘](https://github.com/nodef/extra-iterable/wiki/left)
 * @param x an iterable
 * @param n number of values (1)
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...left(x, 1)];
 * // [ 1 ]
 * 
 * [...left(x, 2)];
 * // [ 1, 2 ]
 * ```
 */
export function left<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Counts the number of values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/length)
 * @param x an iterable
 * @param i start index (0)
 * @param I end index (X)
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * length(x);
 * // 4
 * 
 * length(x, 1);
 * // 3
 * 
 * length(x, 1, 3);
 * // 2
 * ```
 */
export function length<T>(x: Iterable<T>, i?: number, I?: number): number;
/**
 * Converts a once iterable to many.
 * [📘](https://github.com/nodef/extra-iterable/wiki/many)
 * @param x an iterable
 * @param now consume immediately? (false)
 * @example
 * ```javascript
 * var x = [1, 2, 3].values();
 * [...x, ...x];
 * // [ 1, 2, 3 ]
 * // ("x" cant be iterated multiple times)
 * 
 * function* from1ToN(n) {
 *   for(var i=1; i<=n; i++) {
 *     console.log("yielding", i);
 *     yield i;
 *   }
 * }
 * var x = from1ToN(3);
 * var y = many(x);
 * for(var v of y) console.log("read", v);
 * // yielding 1
 * // read 1
 * // yielding 2
 * // read 2
 * // yielding 3
 * // read 3
 * for(var v of y) console.log("read", v);
 * // read 1
 * // read 2
 * // read 3
 * 
 * var x = from1ToN(3);
 * var y = many(x, true);
 * // yielding 1
 * // yielding 2
 * // yielding 3
 * for(var v of y) console.log("read", v);
 * // read 1
 * // read 2
 * // read 3
 * for(var v of y) console.log("read", v);
 * // read 1
 * // read 2
 * // read 3
 * ```
 */
export function many<T>(x: Iterable<T>, now?: boolean): Iterable<T>;
/**
 * Updates values based on map function.
 * [📘](https://github.com/nodef/extra-iterable/wiki/map)
 * @param x an iterable
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * [...map(x, v => v * 2)];
 * // [ 2, 4, 6, 8 ]
 * ```
 */
export function map<T, U>(x: Iterable<T>, fm: mapFn<T, U>): IterableIterator<U>;
/**
 * Finds largest entry.
 * [📘](https://github.com/nodef/extra-iterable/wiki/max)
 * @param x an iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns [index, value]
 * @example
 * ```javascript
 * var x = [1, 2, -3, -4];
 * max(x);
 * // [ 1, 2 ]
 * 
 * max(x, (a, b) => Math.abs(a) - Math.abs(b));
 * // [ 3, -4 ]
 * 
 * max(x, null, v => Math.abs(v));
 * // [ 3, -4 ]
 * ```
 */
export function max<T, U = T>(x: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): [
	number,
	T
];
/**
 * Merges values from sorted iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/merge)
 * @param xs iterables
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 3, 5, 7];
 * var y = [2, 4, 8];
 * [...merge([x, y])];
 * // [
 * //   1, 2, 3, 4,
 * //   5, 7, 8
 * // ]
 * 
 * var y = [-2, -4, -8];
 * [...merge([x, y], (a, b) => Math.abs(a) - Math.abs(b))];
 * // [
 * //   1, -2,  3, -4,
 * //   5,  7, -8
 * // ]
 * 
 * [...merge([x, y], null, v => Math.abs(v))];
 * // [
 * //   1, -2,  3, -4,
 * //   5,  7, -8
 * // ]
 * ```
 */
export function merge<T, U = T>(xs: Iterable<T>[], fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<T>;
/**
 * Gets values from middle.
 * [📘](https://github.com/nodef/extra-iterable/wiki/middle)
 * @param x an iterable
 * @param i start index (0)
 * @param n number of values (1)
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...middle(x, 1, 1)];
 * // [ 2 ]
 * 
 * [...middle(x, 1, 2)];
 * // [ 2, 3 ]
 * ```
 */
export function middle<T>(x: Iterable<T>, i?: number, n?: number): IterableIterator<T>;
/**
 * Finds smallest entry.
 * [📘](https://github.com/nodef/extra-iterable/wiki/min)
 * @param x an iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns [index, value]
 * @example
 * ```javascript
 * var x = [1, 2, -3, -4];
 * min(x);
 * // [ 3, -4 ]
 * 
 * min(x, (a, b) => Math.abs(a) - Math.abs(b));
 * // [ 0, 1 ]
 * 
 * min(x, null, v => Math.abs(v));
 * // [ 0, 1 ]
 * ```
 */
export function min<T, U = T>(x: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): [
	number,
	T
];
/**
 * Moves part of iterable within.
 * [📘](https://github.com/nodef/extra-iterable/wiki/moveWithin)
 * @param x an iterable
 * @param j write index (0)
 * @param i read start index (0)
 * @param I read end index (X)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5, 6];
 * [...moveWithin(x, 0, 1, 3)];
 * // [ 2, 3, 1, 4, 5, 6 ] (2,3 to left)
 * 
 * [...moveWithin(x, 6, 3, 5)];
 * // [ 1, 2, 3, 6, 4, 5 ] (4,5 to right)
 * ```
 */
export function moveWithin<T>(x: Iterable<T>, j?: number, i?: number, I?: number): IterableIterator<T>;
/**
 * Segregates values by test result.
 * [📘](https://github.com/nodef/extra-iterable/wiki/partition)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns [satisfies, doesnt]
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * partition(x, v => v % 2 == 0);
 * // [ [ 2, 4 ], [ 1, 3 ] ]
 * 
 * var x = [1, 2, 3, 4, 5];
 * partition(x, v => v % 2 == 1);
 * // [ [ 1, 3, 5 ], [ 2, 4 ] ]
 * ```
 */
export function partition<T>(x: Iterable<T>, ft: testFn<T>): [
	T[],
	T[]
];
/**
 * Segregates values by similarity.
 * [📘](https://github.com/nodef/extra-iterable/wiki/partitionAs)
 * @param x an iterable
 * @param fm map function (v, i, x)
 * @returns Map {key => values}
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * partitionAs(x, v => v % 2 == 0);
 * // Map(2) { false => [ 1, 3 ], true => [ 2, 4 ] }
 * 
 * var x = [1, 2, 3, 4, 5];
 * partitionAs(x, v => v % 3);
 * // Map(3) { 1 => [ 1, 4 ], 2 => [ 2, 5 ], 0 => [ 3 ] }
 * ```
 */
export function partitionAs<T, U = T>(x: Iterable<T>, fm?: mapFn<T, T | U>): Map<T | U, T[]>;
/**
 * Removes last value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/pop)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * pop(x);
 * // [ 1, 2 ]
 * 
 * var x = [1, 2, 3, 4];
 * pop(x);
 * // [ 1, 2, 3 ]
 * ```
 */
export function pop<T>(x: Iterable<T>): IterableIterator<T>;
/**
 * Adds values to the end.
 * [📘](https://github.com/nodef/extra-iterable/wiki/push)
 * @param x an iterable
 * @param vs values to add
 * @example
 * ```javascript
 * var x = [1, 2];
 * [...push(x, 3)];
 * // [ 1, 2, 3 ]
 * 
 * [...push(x, 3, 4)];
 * // [ 1, 2, 3, 4 ]
 * ```
 */
export function push<T>(x: Iterable<T>, ...vs: T[]): IterableIterator<T>;
/**
 * Finds smallest and largest entries.
 * [📘](https://github.com/nodef/extra-iterable/wiki/range)
 * @param x an iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns [smallest, largest]
 * @example
 * ```javascript
 * var x = [1, 2, -3, -4];
 * range(x);
 * // [ [ 3, -4 ], [ 1, 2 ] ]
 * 
 * range(x, (a, b) => Math.abs(a) - Math.abs(b));
 * // [ [ 0, 1 ], [ 3, -4 ] ]
 * 
 * range(x, null, v => Math.abs(v));
 * // [ [ 0, 1 ], [ 3, -4 ] ]
 * ```
 */
export function range<T, U = T>(x: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): [
	[
		number,
		T
	],
	[
		number,
		T
	]
];
/**
 * Reduces values to a single value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/reduce)
 * @param x an iterable
 * @param fr reduce function (acc, v, i, x)
 * @param acc initial value
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * reduce(x, (acc, v) => acc+v);
 * // 10
 * 
 * reduce(x, (acc, v) => acc+v, 100);
 * // 110
 * ```
 */
export function reduce<T, U>(x: Iterable<T>, fr: reduceFn<T, U>, acc?: U): U;
/**
 * Discards the values which pass a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/reject)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...reject(x, v => v % 2 === 1)];
 * // [ 2, 4 ]
 * 
 * [...reject(x, v => v % 2 === 0)];
 * // [ 1, 3, 5 ]
 * ```
 */
export function reject<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T>;
/**
 * Discards the values at given indices.
 * [📘](https://github.com/nodef/extra-iterable/wiki/rejectAt)
 * @param x an iterable
 * @param is indices
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * [...rejectAt(x, [1, 2])];
 * // [ 2, 8 ]
 * 
 * [...rejectAt(x, [1, 3])];
 * // [ 2, 6 ]
 * ```
 */
export function rejectAt<T>(x: Iterable<T>, is: number[]): IterableIterator<T>;
/**
 * Removes value at index.
 * [📘](https://github.com/nodef/extra-iterable/wiki/remove)
 * @param x an iterable
 * @param i index
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...remove(x, 2)];
 * // [ 1, 2, 4, 5 ]
 * ```
 */
export function remove<T>(x: Iterable<T>, i: number): IterableIterator<T>;
/**
 * Repeats an iterable given times.
 * [📘](https://github.com/nodef/extra-iterable/wiki/repeat)
 * @param x an iterable
 * @param n times (-1 => Inf)
 * @example
 * ```javascript
 * var x = [1, 2];
 * [...repeat(x, 2)];
 * // [ 1, 2, 1, 2 ]
 * 
 * [...repeat(x, 3)];
 * // [ 1, 2, 1, 2, 1, 2 ]
 * ```
 */
export function repeat<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Reverses the values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/reverse)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * [...reverse(x)];
 * // [ 4, 3, 2, 1 ]
 * ```
 */
export function reverse<T>(x: Iterable<T>): IterableIterator<T>;
/**
 * Gets values from right.
 * [📘](https://github.com/nodef/extra-iterable/wiki/right)
 * @param x an iterable
 * @param n number of values (1)
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...right(x, 1)];
 * // [ 3 ]
 * 
 * [...right(x, 2)];
 * // [ 2, 3 ]
 * ```
 */
export function right<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Rotates values in iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/rotate)
 * @param x an iterable
 * @param n rotate amount (+ve: left, -ve: right) (0)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * [...rotate(x, 1)];
 * // [ 2, 3, 4, 1 ]
 * 
 * [...rotate(x, 2)];
 * // [ 3, 4, 1, 2 ]
 * 
 * [...rotate(x, -1)];
 * // [ 4, 1, 2, 3 ]
 * ```
 */
export function rotate<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Scans from left, until a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/scanUntil)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns index where test passes
 * @example
 * ```javascript
 * var x = [1, 1, 2, 2, 3, 3, 4, 4];
 * scanUntil(x, v => v % 2 === 0);
 * // 2           ^
 * 
 * scanUntil(x, v => v % 3 === 0);
 * // 4                 ^
 * 
 * scanUntil(x, v => v % 6 === 0);
 * // 8                            ^
 * ```
 */
export function scanUntil<T>(x: Iterable<T>, ft: testFn<T>): number;
/**
 * Scans from right, until a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/scanUntilRight)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns first index where test fails till end
 * @example
 * ```javascript
 * var x = [1, 1, 2, 2, 3, 3, 4, 4];
 * scanUntilRight(x, v => v % 3 === 0);
 * // 6                       ^
 * 
 * scanUntilRight(x, v => v % 2 === 0);
 * // 8                            ^
 * 
 * scanUntilRight(x, v => v % 6 === 0);
 * // 0    ^
 * ```
 */
export function scanUntilRight<T>(x: Iterable<T>, ft: testFn<T>): number;
/**
 * Scans from left, while a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/scanWhile)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns index where test fails
 * @example
 * ```javascript
 * var x = [1, 1, 2, 2, 3, 3, 4, 4];
 * scanWhile(x, v => v % 2 !== 0);
 * // 2           ^
 * 
 * scanWhile(x, v => v % 3 !== 0);
 * // 4                 ^
 * 
 * scanWhile(x, v => v % 6 !== 0);
 * // 8                            ^
 * ```
 */
export function scanWhile<T>(x: Iterable<T>, ft: testFn<T>): number;
/**
 * Scans from right, while a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/scanWhileRight)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns first index where test passes till end
 * @example
 * ```javascript
 * var x = [1, 1, 2, 2, 3, 3, 4, 4];
 * scanWhileRight(x, v => v % 3 !== 0);
 * // 6                       ^
 * 
 * scanWhileRight(x, v => v % 2 !== 0);
 * // 8                            ^
 * 
 * scanWhileRight(x, v => v % 6 !== 0);
 * // 0    ^
 * ```
 */
export function scanWhileRight<T>(x: Iterable<T>, ft: testFn<T>): number;
/**
 * Finds index of first value passing a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/search)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns index of value, -1 if not found
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * search(x, v => v % 2 == 0);
 * // 1        ^
 * 
 * search(x, v => v % 2 == 1);
 * // 0     ^
 * ```
 */
export function search<T>(x: Iterable<T>, ft: testFn<T>): number;
/**
 * Finds indices of values passing a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchAll)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns indices of value
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...searchAll(x, v => v % 2 == 0)];
 * // [ 1, 3 ] ^     ^
 * 
 * [...searchAll(x, v => v % 2 == 1)];
 * // [ 0, 2, 4 ] ^     ^
 * ```
 */
export function searchAll<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<number>;
/**
 * Finds first index of an infix.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchInfix)
 * @param x an iterable
 * @param y search infix
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var y = [2, 3];
 * searchInfix(x, y);
 * // 1
 * 
 * var y = [-2, -3];
 * searchInfix(x, y);
 * // -1
 * 
 * searchInfix(x, y, (a, b) => Math.abs(a) - Math.abs(b));
 * // 1
 * 
 * searchInfix(x, y, null, v => Math.abs(v));
 * // 1
 * ```
 */
export function searchInfix<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc: compareFn<T | U>, fm?: mapFn<T, T | U>): number;
/**
 * Finds indices of an infix.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchInfixAll)
 * @param x an iterable
 * @param y search infix
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, -2, -3];
 * var y = [2, 3];
 * [...searchInfixAll(x, y)];
 * // [ 1 ]
 * 
 * var y = [-2, -3];
 * [...searchInfixAll(x, y)];
 * // [ 4 ]
 * 
 * [...searchInfixAll(x, y, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ 1, 4 ]
 * 
 * [...searchInfixAll(x, y, null, v => Math.abs(v))];
 * // [ 1, 4 ]
 * ```
 */
export function searchInfixAll<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<number>;
/**
 * Finds last index of an infix.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchInfixRight)
 * @param x an iterable
 * @param y search infix
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var y = [2, 3];
 * searchInfixRight(x, y);
 * // 1
 * 
 * var y = [-2, -3];
 * searchInfixRight(x, y);
 * // -1
 * 
 * searchInfixRight(x, y, (a, b) => Math.abs(a) - Math.abs(b));
 * // 1
 * 
 * searchInfixRight(x, y, null, v => Math.abs(v));
 * // 1
 * ```
 */
export function searchInfixRight<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc: compareFn<T | U>, fm?: mapFn<T, T | U>): number;
/**
 * Finds index of last value passing a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchRight)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @returns index of value, -1 if not found
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * searchRight(x, v => v % 2 == 0);
 * // 3              ^
 * 
 * searchRight(x, v => v % 2 == 1);
 * // 4                 ^
 * ```
 */
export function searchRight<T>(x: Iterable<T>, ft: testFn<T>): number;
/**
 * Finds first index of a subsequence.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchSubsequence)
 * @param x an iterable
 * @param y search subsequence
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns start index of subsequence, -1 if not found
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * searchSubsequence(x, [2, 4]);
 * // 1
 * 
 * searchSubsequence(x, [-2, -4]);
 * // -1
 * 
 * searchSubsequence(x, [-2, -4], (a, b) => Math.abs(a) - Math.abs(b));
 * // 1
 * 
 * searchSubsequence(x, [-2, -4], null, v => Math.abs(v));
 * // 1
 * ```
 */
export function searchSubsequence<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): number;
/**
 * Finds first index of a value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchValue)
 * @param x an iterable
 * @param v search value
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns index of value, -1 if not found
 * @example
 * ```javascript
 * var x = [1, 2, 3, 2, 5];
 * searchValue(x, 2);
 * // 1        ^
 * 
 * var x = [1, -2, 3, 2, 5];
 * searchValue(x, 2, (a, b) => Math.abs(a) - Math.abs(b));
 * // 1         ^
 * 
 * var x = [1, -2, 3, 2, 5];
 * searchValue(x, 2, null, v => Math.abs(v));
 * // 1         ^
 * ```
 */
export function searchValue<T, U = T>(x: Iterable<T>, v: T, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): number;
/**
 * Finds indices of a value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchValueAll)
 * @param x an iterable
 * @param v search value
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns indices of value
 * @example
 * ```javascript
 * var x = [1, 2, 3, 2, 5];
 * [...searchValueAll(x, 2)];
 * // [ 1, 3 ] ^     ^
 * 
 * var x = [1, 2, 3, -2, 5];
 * [...searchValueAll(x, 2, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ 1, 3 ] ^      ^
 * 
 * [...searchValueAll(x, 2, null, v => Math.abs(v))];
 * // [ 1, 3 ] ^      ^
 * ```
 */
export function searchValueAll<T, U = T>(x: Iterable<T>, v: T, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<number>;
/**
 * Finds last index of a value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/searchValueRight)
 * @param x an iterable
 * @param v search value
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @returns index of value, -1 if not found
 * @example
 * ```javascript
 * var x = [1, 2, 3, 2, 5];
 * searchValueRight(x, 2);
 * // 3              ^
 * 
 * var x = [1, 2, 3, -2, 5];
 * searchValueRight(x, 2, (a, b) => Math.abs(a) - Math.abs(b));
 * // 3               ^
 * 
 * searchValueRight(x, 2, null, v => Math.abs(v));
 * // 3               ^
 * ```
 */
export function searchValueRight<T, U = T>(x: Iterable<T>, v: T, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): number;
/**
 * Sets value at index.
 * [📘](https://github.com/nodef/extra-iterable/wiki/set)
 * @param x an iterable
 * @param i index
 * @param v value
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * [...set(x, 1, 40)];
 * // [ 2, 40, 6, 8 ]
 * 
 * [...set(x, 3, 80)];
 * // [ 2, 4, 6, 80 ]
 * ```
 */
export function set<T>(x: Iterable<T>, i: number, v: T): IterableIterator<T>;
/**
 * Removes first value.
 * [📘](https://github.com/nodef/extra-iterable/wiki/shift)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...shift(x)];
 * // [ 2, 3 ]
 * 
 * var x = [1, 2, 3, 4];
 * [...shift(x)];
 * // [ 2, 3, 4 ]
 * ```
 */
export function shift<T>(x: Iterable<T>): IterableIterator<T>;
/**
 * Counts the number of values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/size)
 * @param x an iterable
 * @param i start index (0)
 * @param I end index (X)
 * @example
 * ```javascript
 * var x = [2, 4, 6, 8];
 * size(x);
 * // 4
 * 
 * size(x, 1);
 * // 3
 * 
 * size(x, 1, 3);
 * // 2
 * ```
 */
export function size<T>(x: Iterable<T>, i?: number, I?: number): number;
/**
 * Gets part of an iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/slice)
 * @param x an iterable
 * @param i start index (-ve: from right) (0)
 * @param I end index (-ve: from right) (X)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...slice(x, 2)];
 * // [ 3, 4, 5 ]
 * 
 * [...slice(x, 2, 4)];
 * // [ 3, 4 ]
 * 
 * [...slice(x, -3, -1)];
 * // [ 3, 4 ]
 * ```
 */
export function slice<T>(x: Iterable<T>, i?: number, I?: number): IterableIterator<T>;
/**
 * Checks if any value satisfies a test.
 * [📘](https://github.com/nodef/extra-iterable/wiki/some)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, -3, -4];
 * some(x, v => v > 10);
 * // false
 * 
 * some(x, v => v < 0);
 * // true
 * ```
 */
export function some<T>(x: Iterable<T>, ft?: testFn<T>): boolean;
/**
 * Removes or replaces existing values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/splice)
 * @param x an iterable
 * @param i remove index (0)
 * @param n number of values to remove (rest)
 * @param vs values to insert
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...splice(x, 2)];
 * // [ 1, 2 ]
 * 
 * [...splice(x, 2, 2)];
 * // [ 1, 2, 5 ]
 * 
 * [...splice(x, 2, 2, 30, 40)];
 * // [ 1, 2, 30, 40, 5 ]
 * ```
 */
export function splice<T>(x: Iterable<T>, i?: number, n?: number, ...vs: T[]): IterableIterator<T>;
/**
 * Breaks iterable considering test as separator.
 * [📘](https://github.com/nodef/extra-iterable/wiki/split)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 2, 3, 5, 4, 4, 7];
 * [...split(x, v => v % 2 === 0)];
 * // [ [ 1 ], [ 3, 5 ], [ 7 ] ]
 * 
 * var x = [2, 4, 5, 6, 8];
 * [...split(x, v => v % 2 === 0)];
 * // [ [ 5 ] ]
 * ```
 */
export function split<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T[]>;
/**
 * Breaks iterable considering indices as separator.
 * [📘](https://github.com/nodef/extra-iterable/wiki/splitAt)
 * @param x an iterable
 * @param is indices (sorted)
 * @example
 * ```javascript
 * var x = [1, 2, 2, 3, 5, 4, 4, 7];
 * [...splitAt(x, [1, 2, 5, 6])];
 * // [ [ 1 ], [ 3, 5 ], [ 7 ] ]
 * 
 * var x = [2, 4, 5, 6, 8];
 * [...splitAt(x, [0, 1, 3, 4])];
 * // [ [ 5 ] ]
 * ```
 */
export function splitAt<T>(x: Iterable<T>, is: number[]): IterableIterator<T[]>;
/**
 * Exchanges two values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/swap)
 * @param x an iterable
 * @param i an index
 * @param j another index
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * [...swap(x, 0, 1)];
 * // [ 2, 1, 3, 4 ]
 * 
 * [...swap(x, 0, 3)];
 * // [ 4, 2, 3, 1 ]
 * ```
 */
export function swap<T>(x: Iterable<T>, i: number, j: number): IterableIterator<T>;
/**
 * Gives values not present in both iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/symmetricDifference)
 * @param x an iterable
 * @param y another iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var y = [3, 4, 5];
 * [...symmetricDifference(x, y)];
 * // [ 1, 2, 5 ]
 * 
 * var y = [-3, -4, -5];
 * [...symmetricDifference(x, y)];
 * // [
 * //    1,  2,  3, 4,
 * //   -3, -4, -5
 * // ]
 * 
 * [...symmetricDifference(x, y, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ 1, 2, -5 ]
 * 
 * [...symmetricDifference(x, y, null, v => Math.abs(v))];
 * // [ 1, 2, -5 ]
 * ```
 */
export function symmetricDifference<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<T>;
/**
 * Gets values except first.
 * [📘](https://github.com/nodef/extra-iterable/wiki/tail)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...tail(x)];
 * // [ 2, 3 ]
 * 
 * var x = [1];
 * [...tail(x)];
 * // []
 * ```
 */
export function tail<T>(x: Iterable<T>): IterableIterator<T>;
/**
 * Keeps first n values only.
 * [📘](https://github.com/nodef/extra-iterable/wiki/take)
 * @param x an iterable
 * @param n number of values (1)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...take(x, 2)];
 * // [ 1, 2 ]
 * 
 * [...take(x, 3)];
 * // [ 1, 2, 3 ]
 * ```
 */
export function take<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Keeps last n values only.
 * [📘](https://github.com/nodef/extra-iterable/wiki/takeRight)
 * @param x an iterable
 * @param n number of values (1)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...takeRight(x, 2)];
 * // [ 4, 5 ]
 * 
 * [...takeRight(x, 3)];
 * // [ 3, 4, 5 ]
 * ```
 */
export function takeRight<T>(x: Iterable<T>, n?: number): IterableIterator<T>;
/**
 * Keeps values from left, while a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/takeWhile)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...takeWhile(x, v => v < 3)];
 * // [ 1, 2 ]
 * 
 * [...takeWhile(x, v => v < 4)];
 * // [ 1, 2, 3 ]
 * ```
 */
export function takeWhile<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T>;
/**
 * Keeps values from right, while a test passes.
 * [📘](https://github.com/nodef/extra-iterable/wiki/takeWhileRight)
 * @param x an iterable
 * @param ft test function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 5];
 * [...takeWhileRight(x, v => v >= 3)];
 * // [ 3, 4, 5 ]
 * 
 * [...takeWhileRight(x, v => v >= 4)];
 * // [ 4, 5 ]
 * ```
 */
export function takeWhileRight<T>(x: Iterable<T>, ft: testFn<T>): IterableIterator<T>;
/**
 * Gives values present in any iterable.
 * [📘](https://github.com/nodef/extra-iterable/wiki/union)
 * @param x an iterable
 * @param y another iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4];
 * var y = [3, 4, 5];
 * [...union(x, y)];
 * // [ 1, 2, 3, 4, 5 ]
 * 
 * var y = [-3, -4, -5];
 * [...union(x, y)];
 * // [
 * //    1,  2,  3, 4,
 * //   -3, -4, -5
 * // ]
 * 
 * [...union(x, y, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ 1, 2, 3, 4, -5 ]
 * 
 * [...union(x, y, null, v => Math.abs(v))];
 * // [ 1, 2, 3, 4, -5 ]
 * ```
 */
export function union<T, U = T>(x: Iterable<T>, y: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<T>;
/**
 * Removes duplicate values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/unique)
 * @param x an iterable
 * @param fc compare function (a, b)
 * @param fm map function (v, i, x)
 * @example
 * ```javascript
 * var x = [1, 2, 3, 4, 2, 3];
 * [...unique(x)];
 * // [ 1, 2, 3, 4 ]
 * 
 * var x = [1, 2, 3, 4, -2, -3];
 * [...unique(x)];
 * // [ 1, 2, 3, 4, -2, -3 ]
 * 
 * [...unique(x, (a, b) => Math.abs(a) - Math.abs(b))];
 * // [ 1, 2, 3, 4 ]
 * 
 * [...unique(x, null, v => Math.abs(v))];
 * // [ 1, 2, 3, 4 ]
 * ```
 */
export function unique<T, U = T>(x: Iterable<T>, fc?: compareFn<T | U>, fm?: mapFn<T, T | U>): IterableIterator<T>;
/**
 * Adds values to the start.
 * [📘](https://github.com/nodef/extra-iterable/wiki/unshift)
 * @param x an iterable
 * @param vs values to add
 * @example
 * ```javascript
 * var x = [3, 4];
 * [...unshift(x, 2)];
 * // [ 2, 3, 4 ]
 * 
 * [...unshift(x, 1, 2)];
 * // [ 1, 2, 3, 4 ]
 * ```
 */
export function unshift<T>(x: Iterable<T>, ...vs: T[]): IterableIterator<T>;
/**
 * Lists all values.
 * [📘](https://github.com/nodef/extra-iterable/wiki/values)
 * @param x an iterable
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * [...values(x)];
 * // [ 1, 2, 3 ]
 * ```
 */
export function values<T>(x: Iterable<T>): IterableIterator<T>;
/**
 * Combines values from iterables.
 * [📘](https://github.com/nodef/extra-iterable/wiki/zip)
 * @param xs iterables
 * @param fm map function (vs, i, xs)
 * @param ft till function (dones) (some)
 * @param vd default value
 * @example
 * ```javascript
 * var x = [1, 2, 3];
 * var y = [4, 5];
 * [...zip([x, y])];
 * // [ [ 1, 4 ], [ 2, 5 ] ] (shortest)
 * 
 * [...zip([x, y], ([a, b]) => a + b)];
 * // [ 5, 7 ]
 * 
 * [...zip([x, y], null, some)];
 * // [ [ 1, 4 ], [ 2, 5 ] ] (shortest)
 * 
 * [...zip([x, y], null, every, 0)];
 * // [ [ 1, 4 ], [ 2, 5 ], [ 3, 0 ] ] (longest)
 * 
 * [...zip([x, y], null, head, 0)];
 * // [ [ 1, 4 ], [ 2, 5 ], [ 3, 0 ] ] (first)
 * ```
 */
export function zip<T, U = T[]>(xs: Iterable<T>[], fm?: mapFn<T[], T[] | U>, ft?: tillFn, vd?: T): IterableIterator<T[] | U>;

export {};
}
